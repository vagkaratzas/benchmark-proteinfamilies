nextflow_process {

    name "Test Process DOWNLOAD_HAMAP"
    script "../main.nf"
    process "DOWNLOAD_HAMAP"

    // Stub only: the script block downloads a remote reference database.
    // Running it for real would fetch tens of GB on every test run.

    test("no input - remote database - hamap alignments - stub") {

        options "-stub"

        then {
            assertAll(
                { assert process.success },
                { assert snapshot(process.out).match() }
            )
        }

    }

}
